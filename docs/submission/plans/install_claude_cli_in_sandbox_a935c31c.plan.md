---
name: Install claude CLI in sandbox
overview: Add `npm install -g @anthropic-ai/claude-code` to the E2B sandbox template so the `claude` CLI is available, which is required for the Claude Agent SDK's `query()` to function and emit session IDs.
todos: []
isProject: false
---

# Install Claude Code
